jQuery 정리!
- javascript를 쉽게 구현한 언어.
- 배우기 쉽다!

자바스크립트를  간단하게  정리가 가능하다.!

jquery다운은 cdn방식을 사용하기로 함

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>

기본 문법 $(selector).action()이며 selector에 있는거에 대해 action한다. 
fail examples!
Trying to hide an element that is not created yet. - 아직 생성되지 않은 엘레멘트를 숨기려 할 때
Trying to get the size of an image that is not loaded yet  -아직 로드되지 않은 이미지파일의 크기를 가져올 때.


element selector = "asd" type.  "this" = current html element.
#id selector = "#id" type.
.class selector = #.test" type.

I practiced anoter examples that w3scools provide. (ex1~6)

